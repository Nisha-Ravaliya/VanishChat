# App name
spring.application.name=VanishChat

# Server port (optional)
server.port=8080


#Database Configuration

spring.datasource.url=jdbc:mysql://localhost:3306/vanish_chat
spring.datasource.username=root
spring.datasource.password=admin@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.transaction.jta.platform=org.hibernate.engine.transaction.jta.platform.internal.NoJtaPlatform


# Thymeleaf Template Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.check-template-location=true
spring.mvc.view.prefix=/templates/
spring.mvc.view.suffix=.html

# =======================
# ? Spring Security (basic for dev)
# =======================
spring.security.user.name=nisha
spring.security.user.password=nisha@2005
spring.security.user.roles=ADMIN

# To disable Spring Security uncomment the below line
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration

jwt.secret=vanish_chat_vanish_chat_vanish_chat_vanish_chat
jwt.expiration=3600000
# File Upload

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Upload directory (used in your service)
file.upload-dir=upload


#JWT (if using later)

app.jwt.secret=very_secret_key
app.jwt.expiration=3600000
